pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Perform your build tasks here
                echo 'Building the project...'
            }
        }
        
        stage('Test') {
            steps {
                // Perform your testing tasks here
                echo 'Running tests...'
            }
        }
        
        stage('Deploy') {
            steps {
                // Perform deployment tasks here
                echo 'Deploying the application...'
            }
        }
    }
}

